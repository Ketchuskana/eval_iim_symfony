{% extends 'base.html.twig' %}

{% block title %}{{ produit.nom }}{% endblock %}

{% block body %}
    <h1>{{ produit.nom }}</h1>
    <p>{{ produit.description }}</p>
    <p>Prix : {{ produit.prix }} points</p>

    <a href="{{ path('produit_acheter', {'id': produit.id}) }}" class="btn btn-primary">Acheter</a>
{% endblock %}
